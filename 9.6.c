#include<stdio.h>
#include<conio.h>
int  main()
{
    int x=20;
    while (x>=1)
      {
        if(x%2==0)
        printf(" ");
        else
        printf("%d",x);

        x--;
      }
     getch();

 }
